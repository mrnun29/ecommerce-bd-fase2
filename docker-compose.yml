version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: ecommerce_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: ecommerce_db
      MYSQL_USER: ecommerce_user
      MYSQL_PASSWORD: ecommerce_pass
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
      - ./schema_refactor.sql:/docker-entrypoint-initdb.d/01-schema.sql
      - ./seed_refactor.py:/docker-entrypoint-initdb.d/02-seed.py
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  web:
    build: .
    container_name: ecommerce_web
    restart: always
    ports:
      - "5001:5001"
    environment:
      DB_HOST: mysql
      DB_USER: ecommerce_user
      DB_PASSWORD: ecommerce_pass
      DB_NAME: ecommerce_db
      FLASK_ENV: development
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - .:/app
    command: sh -c "sleep 5 && python seed_refactor.py && python app.py"

volumes:
  mysql_data:
